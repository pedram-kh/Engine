/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Published Laravel mail views (Sprint 3.5 Chunk 4 — Catalyst Engine v2 brand mail
# theme). The repo-root .gitignore `vendor/` rule (intended for Composer
# dependencies) also matches this published path; re-include it so the brand
# mail theme + the vendor blade templates it depends on are committed.
!resources/views/vendor/
!resources/views/vendor/**
