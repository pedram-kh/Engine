# Copy to production.tfvars (gitignored) and fill in real values before
# running `terraform plan -var-file=production.tfvars` for production.

owner_email = "engineering@catalystengine.example"
cost_center = "engineering"
domain      = "catalystengine.example"
