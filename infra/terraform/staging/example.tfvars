# Copy to staging.tfvars (gitignored) and fill in real values before
# running `terraform plan -var-file=staging.tfvars` for staging.

owner_email = "engineering@catalystengine.example"
cost_center = "engineering"
domain      = "staging.catalystengine.example"
